Changes in component 'nf-core/seqkit/replace'
'modules/nf-core/seqkit/replace/environment.yml' is unchanged
'modules/nf-core/seqkit/replace/meta.yml' is unchanged
Changes in 'seqkit/replace/main.nf':
--- modules/nf-core/seqkit/replace/main.nf
+++ modules/nf-core/seqkit/replace/main.nf
@@ -1,38 +1,36 @@
 process SEQKIT_REPLACE {
-    tag "$meta.id"
+    tag "${meta.id}"
     label 'process_low'
 
     conda "${moduleDir}/environment.yml"
-    container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
-        'https://depot.galaxyproject.org/singularity/seqkit:2.9.0--h9ee0642_0':
-        'biocontainers/seqkit:2.9.0--h9ee0642_0' }"
+    container "${workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container
+        ? 'https://depot.galaxyproject.org/singularity/seqkit:2.9.0--h9ee0642_0'
+        : 'biocontainers/seqkit:2.9.0--h9ee0642_0'}"
 
     input:
     tuple val(meta), path(fastx)
 
     output:
-    tuple val(meta), path("*.fast*"), emit: fastx
-    path "versions.yml"             , emit: versions
+    tuple val(meta), path("${prefix}"), emit: fastx
+    path "versions.yml", emit: versions
 
     when:
     task.ext.when == null || task.ext.when
 
     script:
     def args = task.ext.args ?: ''
-    def prefix = task.ext.prefix ?: "${meta.id}"
     def extension = "fastq"
-    if ("$fastx" ==~ /.+\.fasta|.+\.fasta.gz|.+\.fa|.+\.fa.gz|.+\.fas|.+\.fas.gz|.+\.fna|.+\.fna.gz/) {
+    if ("${fastx}" ==~ /.+\.fasta|.+\.fasta.gz|.+\.fa|.+\.fa.gz|.+\.fas|.+\.fas.gz|.+\.fna|.+\.fna.gz|.+\.faa.gz|.+\.faa/) {
         extension = "fasta"
     }
-    def endswith = task.ext.suffix ?: "${extension}.gz"
+    prefix = task.ext.prefix ?: "${meta.id}.${extension}"
     """
     seqkit \\
         replace \\
         ${args} \\
         --threads ${task.cpus} \\
         -i ${fastx} \\
-        -o ${prefix}.${endswith}
-
+        -o ${prefix}
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":
         seqkit: \$( seqkit version | sed 's/seqkit v//' )
@@ -40,20 +38,18 @@
     """
 
     stub:
-    def prefix = task.ext.prefix ?: "${meta.id}"
+    prefix = task.ext.prefix ?: "${meta.id}"
     def extension = "fastq"
-    if ("$fastx" ==~ /.+\.fasta|.+\.fasta.gz|.+\.fa|.+\.fa.gz|.+\.fas|.+\.fas.gz|.+\.fna|.+\.fna.gz/) {
+    if ("${fastx}" ==~ /.+\.fasta|.+\.fasta.gz|.+\.fa|.+\.fa.gz|.+\.fas|.+\.fas.gz|.+\.fna|.+\.fna.gz|.+\.faa.gz|.+\.faa/) {
         extension = "fasta"
     }
-    def endswith = task.ext.suffix ?: "${extension}.gz"
-
+    prefix = task.ext.prefix ?: "${meta.id}.${extension}"
     """
-    echo "" | gzip > ${prefix}.${endswith}
+    echo "" | gzip > ${prefix}
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":
         seqkit: \$( seqkit version | sed 's/seqkit v//' )
     END_VERSIONS
     """
-
 }

'modules/nf-core/seqkit/replace/tests/main.nf.test' is unchanged
'modules/nf-core/seqkit/replace/tests/tags.yml' is unchanged
'modules/nf-core/seqkit/replace/tests/main.nf.test.snap' is unchanged
'modules/nf-core/seqkit/replace/tests/uncomp.config' is unchanged
'modules/nf-core/seqkit/replace/tests/replace.config' is unchanged
************************************************************
